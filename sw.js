self.addEventListener('install', (e) => {
    console.log('[Service Worker] Installed');
});

self.addEventListener('fetch', (e) => {
    // V2این یک سرویس‌ورکر ساده برای فعال‌سازی دکمه نصب است
});
